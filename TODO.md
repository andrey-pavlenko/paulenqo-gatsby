- ~~replace in github action `npm run clean && npm run build` to `npm run build`~~
- ~~replace in github action `npm install` to `npm ci`~~
- do single product pages
  - open-graph meta-data
  - gallery slider
  - buy button
    - what this button will do?
